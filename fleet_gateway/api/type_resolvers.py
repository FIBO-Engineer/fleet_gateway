"""
GraphQL field resolvers for Fleet Gateway types.

Resolvers have been moved inline as @strawberry.field methods on each type
in types.py to avoid the circular import between types.py and this module.
"""
